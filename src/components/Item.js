import React from "react";
import PropTypes from "prop-types";

const Item = props => {
  return (
    <div>
      <input type="checkbox" />
    </div>
  );
};

Item.propTypes = {};

export default Item;
